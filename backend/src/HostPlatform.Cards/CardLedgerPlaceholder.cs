namespace HostPlatform.Cards;

/// <summary>Ledger operations must run in simulation mode until certifications exist.</summary>
public static class CardLedgerPlaceholder
{
    public const bool DefaultSimulation = true;
}
