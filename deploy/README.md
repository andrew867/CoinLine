# Deploy

Use this folder for **customer-specific** deployment templates (Helm, Kustomize, Terraform snippets) that are safe to share. Do not commit production secrets.

For baseline Docker Compose, start with `../docker/docker-compose.yml`.
