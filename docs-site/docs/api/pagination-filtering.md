# Pagination & filtering

**Audit** (`GET /api/audit/events`): `page`, `pageSize`, `sort`, `q`, `category`, `terminalId` — bounded by `Platform:QueryLimits`.

Other list endpoints return arrays or filtered query params — see each endpoint doc and OpenAPI parameters.
