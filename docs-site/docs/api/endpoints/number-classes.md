# Number classes API

**Route prefix:** `/api/number-classes`

| Method | Path |
|--------|------|
| GET | `/api/number-classes` |
| POST | `/api/number-classes` | Blocked-class confirm |

Also referenced from rating quote flows — see [Rating](rating.md).

**UI:** `/number-classes`
