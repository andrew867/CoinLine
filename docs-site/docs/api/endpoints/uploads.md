# Uploads API

**Route prefix:** `/api/uploads`

| Method | Path |
|--------|------|
| GET | `/api/uploads` |
| GET | `/api/uploads/{id}` |
| POST | `/api/uploads` |

**UI:** `/uploads` (JsonPanel)

**Entities:** `UploadBatch`, `UploadRecord`
