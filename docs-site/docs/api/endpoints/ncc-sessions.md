# NCC sessions API

**Route prefix:** `/api/ncc/sessions`

| Method | Path | Purpose |
|--------|------|---------|
| GET | `/api/ncc/sessions` | List NCC sessions |

**Related UI:** `/ncc-sessions` (JsonPanel)

**Entity:** `NccSession`
