# Terminal events API

Terminal lifecycle events are exposed via **`GET /api/terminals/{id}/events`** (see [Terminals](terminals.md)).

Underlying entity: **`TerminalEvent`** (audit-style timeline).

**Related UI:** Terminal detail page sections consuming events JSON.
