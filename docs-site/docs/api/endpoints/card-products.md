# Card products API

**Route prefix:** `/api/cards/products`

(See Cards controller — nested under `/api/cards`.)

| Method | Path |
|--------|------|
| GET | `/api/cards/products` |
| GET | `/api/cards/products/{id}` |
| POST | `/api/cards/products` |

**SIMULATION ONLY** defaults — see `GET /api/cards/simulation-state`.

**UI:** `/card-products`
