# Audit API

**Route:** `GET /api/audit/events`

Query filters: terminal, category, text search, pagination — see [Pagination](../pagination-filtering.md).

**UI:** `/audit`

**Entity:** `AuditEvent`
