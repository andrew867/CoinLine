# Versioning

Single OpenAPI document **`v1`**. Breaking changes should bump route prefix or negotiate explicit API version header — **current codebase uses unversioned `/api`** (document as stability caveat for integrators).
