# Field validation overview

Technician-oriented validation — complements repository **`docs/host_platform/hw_validation/`** (checklists, capture workflows).

All flows assume **real evidence** — no substitute for modem/UART captures where claims involve on-wire behavior.
