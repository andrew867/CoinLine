# Modem transport

Collect AT traces, carrier timings, retrains — correlate with host session correlation ids.

See repository modem checklist path referenced from **`GET /api/hw-validation/checklists`**.
