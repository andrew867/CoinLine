# Install

1. Install .NET 9, Node 20, PostgreSQL 16+ (or container).
2. Clone repository; `cd coinline/backend` && `dotnet restore`.
3. Apply migrations (see [Migrations](../backend/migrations.md)).
