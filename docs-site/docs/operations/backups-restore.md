# Backups & restore

Use organization backup policy. Database contains **AuditEvents**, **DlogTransactions** (sensitive), card token references, firmware metadata.

Test restore in staging before DR event.
