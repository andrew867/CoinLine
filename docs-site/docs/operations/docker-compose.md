# Docker Compose

See `docker/` — production overlay examples set **`Production`** env and disable demo seed.

!!! warning "Secrets"
    Provide **`HOSTPLATFORM_API_KEYS`** and Postgres credentials via compose secrets — never commit real keys.
