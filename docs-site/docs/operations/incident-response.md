# Incident response

1. Identify blast radius (terminals, data classes).
2. Rotate **API keys** if credential leak suspected.
3. Preserve **AuditEvents** and DB backups before destructive fixes.
4. For protocol incidents, export DLOG/NCC captures with audit trail.
