# Operations overview

Runbook-style operations: install, configure, deploy, monitor, upgrade, rollback.

Start: [Install](install.md), [Configuration](configuration.md), [Docker Compose](docker-compose.md).
