# Event reference

- **TerminalEvent** — operational history rows linked to terminal.
- **AuditEvent** — security/operator audit stream (categories in implementation notes).
- **CraftAuditEvent** — craft-specific trail.
