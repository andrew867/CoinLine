# Error codes (application)

Protocol diagnostics use string codes — examples:

- **EMPTY_PAYLOAD** (DLOG classifier)
- **UNKNOWN_MT**
- **EMPTY_PAYLOAD** (table validation)

Domain exceptions may surface as problem details — prefer OpenAPI + integration tests for exact bodies.
