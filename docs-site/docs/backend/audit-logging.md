# Audit logging

`AuditEvents` rows: **category**, **action**, **actor**, **resource**, **DetailJson**, optional **TerminalId**, **CorrelationId**.

Categories include **`dlog`**, **`tables.*`**, **`rating`**, **`cards`**, **`craft`**, **`firmware`**, **`ncc.frame_capture`**, **`hw_validation.captured_session`**.
