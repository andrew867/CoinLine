# Observability

- **Logging:** JSON console (non-Testing), correlation id middleware.
- **Metrics:** OpenTelemetry Prometheus exporter optional (`GET /metrics`).
- **Health:** `/health`, `/health/live`, `/ready`, `/health/ready`.
