# Seed data

`SeedData.EnsureSeedAsync` — DLOG message types, craft opcodes, demo customer/terminal when **`Platform:Seed:EnableDemoData`** is true.

Disable demo seed in production overlays (`docker-compose.prod` examples).
