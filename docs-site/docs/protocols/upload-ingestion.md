# Upload ingestion

Upload batches/records correlate terminal→host transfers — pairing with DLOG rows optional.

See `UploadsController` and domain entities — policy-specific semantics **`OPEN QUESTION`** per SKU.
