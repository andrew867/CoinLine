# HARDWARE VALIDATION REQUIRED

This badge means **lack of certified evidence** for live modem/UART/EEPROM/PCI behavior — **not** a software bug by itself.

Remediation paths:

1. Capture terminal traces + import envelopes.
2. Extend golden fixtures with **`HARDWARE_CAPTURED`** lineage only when archives exist.
3. Signed field acceptance document.
