# Table distribution

Opaque blobs stored with **SHA-256** and length — **no firmware struct parsing** in API responses.

Validation helper surfaces **`EMPTY_PAYLOAD`** for zero-byte payloads.

Download orchestration tracks batches/items — terminal-side ACK **`HARDWARE VALIDATION REQUIRED`**.
