# Fixtures & replay

- **Golden fixtures:** `fixtures/` with manifests (`*.fixture.json`).
- **Replay:** `POST /api/dlog/replay` for DB export; **`POST /api/hw-validation/.../replay`** for imported envelopes.

See [Hardware validation tests](../testing/hardware-validation-tests.md).
