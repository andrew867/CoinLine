# Raw payload preservation

Principle: **never discard unknown octets**. DLOG transactions retain full bytes; diagnostics explain uncertainty.

Dangerous operations (replay export) require explicit operator confirmation + audit.
