# Firmware update

Registry holds packages/artifacts/checksums — jobs orchestrate **simulation-first** states.

Live XMODEM/DLA **`HARDWARE VALIDATION REQUIRED`** (`IDlXmodemTransportAdapter`).

See repository `docs/protocols/firmware_update/` and `FirmwareJobOrchestrator`.
