# Domain model

Core aggregates include **Customer**, **Site**, **Terminal**, **TableDefinition** / **TableVersion** / **TableSet**, **DlogTransaction**, **NccSession**, **RatePlan** / **RatePlanVersion** / **RateRule**, **CallRecord**, card accounts/transactions, **CraftSession** / **CraftCommand**, **FirmwarePackage** / **FirmwareUpdateJob**.

Full table list: [Entity reference](../reference/entity-reference.md).
