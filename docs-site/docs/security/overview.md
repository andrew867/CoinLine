# Security overview

Defense in depth: **API keys**, **RBAC**, **audit logging**, **payload handling policy**, **simulation defaults** for dangerous subsystems.

See [Authentication](authn-authz.md), [Threat model](threat-model.md).
