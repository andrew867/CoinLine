# Authentication & authorization

- **ApiKeyGateMiddleware** — **`X-API-Key`** when configured.
- **Policies** — Operator / Technician / Admin minimum roles.

Development bypass noted in tests — **never enable in production**.
