# Roles & permissions

| Policy | Typical use |
|--------|-------------|
| RequireOperator | Mutating operational APIs |
| RequireTechnician | Elevated craft/firmware routes |
| RequireAdmin | Approvals (firmware job approve) |

Exact `[Authorize]` attributes vary — verify OpenAPI + controller source.
