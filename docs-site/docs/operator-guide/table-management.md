# Table management

1. Define tables (`POST /api/tables/definitions`).
2. Create versions with opaque payload (`POST /api/tables/versions`).
3. Bundle table sets (`POST /api/tables/sets`).
4. Publish (`POST .../publish` with confirm).

**UI:** `/table-definitions`, `/table-sets`
