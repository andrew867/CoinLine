# Terminals

1. Register terminal (`POST /api/terminals`) with site + terminal id hex.
2. Observe status (`POST .../status`).
3. Assign table set (`POST .../table-assignment` with confirms).

**UI:** `/terminals`, `/terminals/new`
