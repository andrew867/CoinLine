# Tests (package index)

Automated tests ship with **CoinLine Server** and **CoinLine Management Console** source:

- `backend/tests/` — .NET unit, integration, protocol, and golden tests
- `web/` — `npm test`, `npm run e2e` (Playwright)

There are no additional top-level test projects in this folder by default.
